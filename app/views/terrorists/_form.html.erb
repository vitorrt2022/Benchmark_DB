<%= form_with(model: terrorist) do |form| %>
  <% if terrorist.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(terrorist.errors.count, "error") %> prohibited this terrorist from being saved:</h2>

      <ul>
        <% terrorist.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :id, style: "display: block" %>
    <%= form.number_field :id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
